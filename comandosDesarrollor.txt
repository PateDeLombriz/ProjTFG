

//Inserir clau hahejada
docker compose exec django-web python manage.py shell -c "from django.contrib.auth.hashers import make_password; from apiApp.models import Usuari, Contrasenya; u=Usuari.objects.get(loginField='info@alzina.com'); e=u.empresa; pwd=(Contrasenya.objects.filter(id_empresa=e, data_reemplas__isnull=True).order_by('-data_creacio').first() or Contrasenya.objects.filter(id_empresa=e).order_by('-data_creacio').first()); pwd.clau=make_password('Alzina'); pwd.save(update_fields=['clau']); print({'pwd_id': pwd.id, 'clau_actualitzada': pwd.clau})"
docker compose exec django-web python manage.py shell -c "from django.contrib.auth.hashers import make_password; from apiApp.models import Usuari, Contrasenya; u=Usuari.objects.get(loginField='MartaSport'); e=u.treballador; pwd=(Contrasenya.objects.filter(id_treballador=e, data_reemplas__isnull=True).order_by('-data_creacio').first() or Contrasenya.objects.filter(id_treballador=e).order_by('-data_creacio').first()); pwd.clau=make_password('12345678'); pwd.save(update_fields=['clau']); print({'pwd_id': pwd.id, 'clau_actualitzada': pwd.clau})"
docker compose exec django-web python manage.py shell -c "from django.contrib.auth.hashers import make_password; from apiApp.models import Usuari, Contrasenya; u=Usuari.objects.get(loginField='PaulaCable'); e=u.treballador; pwd=(Contrasenya.objects.filter(id_treballador=e, data_reemplas__isnull=True).order_by('-data_creacio').first() or Contrasenya.objects.filter(id_treballador=e).order_by('-data_creacio').first()); pwd.clau=make_password('Cable2020'); pwd.save(update_fields=['clau']); print({'pwd_id': pwd.id, 'clau_actualitzada': pwd.clau})"
docker compose exec django-web python manage.py shell -c "from django.contrib.auth.hashers import make_password; from apiApp.models import Usuari, Contrasenya; u=Usuari.objects.get(loginField='JohanPaletas'); e=u.treballador; pwd=(Contrasenya.objects.filter(id_treballador=e, data_reemplas__isnull=True).order_by('-data_creacio').first() or Contrasenya.objects.filter(id_treballador=e).order_by('-data_creacio').first()); pwd.clau=make_password('Paletas'); pwd.save(update_fields=['clau']); print({'pwd_id': pwd.id, 'clau_actualitzada': pwd.clau})"